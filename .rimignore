# .rimignore — Fortified Feature Framework
# 上傳 Steam 創意工坊時要排除的檔案／資料夾。
#
# 語法與 GitHub 的 .gitignore 完全相同：
#   #        註解（\# 可轉義為字面井號）
#   !xxx     反選（重新納入），後出現的規則覆蓋先出現的規則
#   /xxx     錨定於模組根目錄
#   xxx/     僅匹配目錄
#   *        任意字元（不跨越 /）
#   ?        單一字元（不跨越 /）
#   [abc]    字元集，[!abc] 為排除
#   **/xxx   任意深度；xxx/** 代表該目錄底下全部
#
# 注意：與 Git 相同，資料夾一旦被排除，其中的內容就無法再用 ! 重新納入。
#       若要「排除資料夾內容但保留其中一兩個檔案」，請寫成 /Docs/* 加上 !/Docs/Manual.md。
#
# 內建已自動排除（可用 ! 覆寫）：
#   本檔案、.git/ .gitignore .gitattributes .svn/ .hg/
#   .vs/ .idea/ .vscode/ Thumbs.db desktop.ini .DS_Store
#
# 上傳前會跳出預覽視窗列出所有被排除的項目；
# 開發者模式下可用 Debug actions → Fortified →「預覽 .rimignore 排除結果」隨時 dry-run。


# ═══════════════════════════════════════════════════════════
#  覆寫內建預設：本檔案要一起上傳
#  FF 是給其他模組作者使用的框架，把 .rimignore 一併發佈可以
#  當成現成範例；同時也示範了「用 ! 覆寫內建規則」的寫法。
# ═══════════════════════════════════════════════════════════
!.rimignore

# ═══════════════════════════════════════════════════════════
#  原始碼與建置產物
#  Fortified.dll / FortifiedCE.dll 已編譯進 1.6/Assemblies，
#  訂閱者不需要 35 MB 的 C# 專案與 NuGet 快取。
# ═══════════════════════════════════════════════════════════
/_Sources/
/Source/
**/obj/
**/bin/
*.pdb
*.mdb
*.csproj.user
*.sln
*.suo
*.user
*.kra
*.~kra

# ═══════════════════════════════════════════════════════════
#  Unity 著色器專案
#  已建置為 1.6/AssetBundles/fortified_shaders_{win,mac,linux}，
#  原始 .shader 與 Library/ 不需隨模組發佈。
# ═══════════════════════════════════════════════════════════
/UnityProject/

# AssetBundle 的 .manifest 是 Unity 建置中繼資料。
# RimWorld 的 ModAssetBundlesHandler 只載入「沒有副檔名」的檔案，
# 因此 .manifest 一定不會被讀取，可安全排除。
/1.6/AssetBundles/*.manifest

# ═══════════════════════════════════════════════════════════
#  開發用工具、腳本與其輸出報告
# ═══════════════════════════════════════════════════════════
/_Tools/
/.claude/
/build.bat
*.lnk
*.bat
*.ps1
*.sh

# ═══════════════════════════════════════════════════════════
#  貼圖最佳化快取
#  RimPy / png2dds 產生的 .dds 會在訂閱者端自行重建，
#  隨模組上傳只會讓體積翻倍。
# ═══════════════════════════════════════════════════════════
*.dds

# ═══════════════════════════════════════════════════════════
#  編輯器、備份與壓縮檔雜訊
# ═══════════════════════════════════════════════════════════
*.orig
*.rej
*.bak
*.tmp
*.log
*~
.editorconfig
*.zip
*.7z
*.rar


# ═══════════════════════════════════════════════════════════
#  以下為選用規則，預設保留。需要時把行首的 # 拿掉即可。
# ═══════════════════════════════════════════════════════════

# 開發／測試用 Def（會在遊戲內出現除錯用的物件與 Gizmo）
# /1.6/Defs/DebugDefs.xml
# /1.6/Defs/TestDefs.xml

# 框架使用範例文件（提供給其他模組作者參考，一般建議保留）
# /DamageBlockerExamples.xml
# /CHANGELOG.md

# 1.5 佔位資料夾（只有一份說明 readme，用於滿足依賴檢查）
# /1.5/
